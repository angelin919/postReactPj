import React from 'react';

const Header = () => {
    return (
        <header style={{ background: '#333', color: '#fff', padding: '1rem' }}>
            <h1>Post App</h1>
        </header>
    );
};

export default Header;