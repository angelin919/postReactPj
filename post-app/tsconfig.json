{
  "files": [],                          // Пустой массив = не проверяем файлы напрямую
  
  "references": [
    { "path": "./tsconfig.app.json" }, // React приложение
    { "path": "./tsconfig.node.json" }  
  ],
  
  "exclude": ["node_modules"]           // Не проверять node_modules
}
